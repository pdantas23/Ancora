// ─── Autenticação ────────────────────────────────────────────────────────────
export const AUTH_COOKIE_ACCESS  = "access_token";
export const AUTH_COOKIE_REFRESH = "refresh_token";

// ─── Tempo (ms) ──────────────────────────────────────────────────────────────
export const ONE_HOUR_MS  = 1000 * 60 * 60;
export const ONE_DAY_MS   = ONE_HOUR_MS * 24;
export const ONE_WEEK_MS  = ONE_DAY_MS * 7;
export const ONE_MONTH_MS = ONE_DAY_MS * 30;
export const ONE_YEAR_MS  = ONE_DAY_MS * 365;

// ─── Paginação padrão ────────────────────────────────────────────────────────
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE     = 100;

// ─── App ─────────────────────────────────────────────────────────────────────
export const APP_NAME        = "Âncora";
export const APP_DESCRIPTION = "Plataforma unificada Âncora — Consórcio e Seguro";

// ─── Tabelas de leads ─────────────────────────────────────────────────────────
export const TABLE_LEADS_CONSORCIO = "leads_ancora_consorcio";
export const TABLE_LEADS_SEGURO    = "leads_ancora_seguro";

// ─── Tabela de perfis ─────────────────────────────────────────────────────────
export const TABLE_PROFILES = "profiles_ancora";
